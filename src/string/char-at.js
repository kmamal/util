
const charAt = (str, i) => str.charAt(i)
const charCodeAt = (str, i) => str.charCodeAt(i)
const charPointAt = (str, i) => str.charPointAt(i)

module.exports = {
	charAt,
	charCodeAt,
	charPointAt,
}
